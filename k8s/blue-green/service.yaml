apiVersion: v1
kind: Service
metadata:
  name: svc-bluegreen
spec:
  type: LoadBalancer
  selector:
    app: demo
    version: blue        # cambiamos a "green" con kubectl patch
  ports:
    - port: 80
      targetPort: 80
